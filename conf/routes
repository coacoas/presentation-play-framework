# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.TwitterClient.index()
GET     /search                     controllers.TwitterClient.search(query)
GET     /stream/:query              controllers.TwitterClient.filter(query)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /jsRoutes                   controllers.TwitterClient.jsRoutes
